{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "POC capability: core + updater + deep-link permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "updater:default",
    "deep-link:default"
  ]
}
