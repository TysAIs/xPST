<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Entitlements for the bundled Python engine (PyInstaller onedir) and any other
	     nested executable the app spawns. The engine is a separate process, so it needs
	     its own JIT/executable-memory allowance, and it loads native extension modules
	     from its own directory, which hardened runtime rejects without
	     disable-library-validation. It is signed with the same identity as the app. -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
